\### \*\*Day 9 — 스킬 레벨업 실제 효과 적용\*\*



| \*\*파일\*\* | \*\*작업 내용\*\* |

| --- | --- |

| `SkillData.cs` | `SkillID` 열거형 + `SkillLevelStats` 구조체 추가 (레벨별 수치 정의) |

| `SkillEffectApplier.cs` | 신규 생성. `LevelManager.OnSkillSelected` 수신 → 스킬별 스탯 실제 적용 |

| `LevelManager.cs` | `OnSkillSelected` 이벤트 추가, `SelectSkill()` 업데이트 |

| `PlayerStats.cs` | `hpRegenPerSecond` 추가 (세포 재생 패시브용, Update에서 자동 회복) |

| `Slash.cs` | `knockbackEnabled` 플래그 + 넉백 로직 추가 (Lv4) |



\---



\### \*\*Day 10 — 투사체 시스템 + 피격 피드백\*\*



| \*\*파일\*\* | \*\*작업 내용\*\* |

| --- | --- |

| `Projectile.cs` | 신규 생성. 공통 투사체 — 방향 이동, 수명 관리, 충돌 피해, 오브젝트 풀 |

| `PoisonNeedle.cs` | 신규 생성. 자체 쿨타임/탐색/풀 내장. Lv4: 3발 부채꼴 + 관통 |

| `HitEffect.cs` | 신규 생성. 피격 시 흰색 색상 깜빡임 |

| `EnemyBase.cs` | `HitEffect` 연결 — `TakeDamage()` 시 플래시 발동 |

| `SkillData.cs` | `cooldownDelta`, `piercingEnabled`, `extraProjectiles` 필드 추가 |

| `SkillEffectApplier.cs` | `PoisonNeedle` 케이스 추가 |



\---



\### \*\*Day 11 — 공격 스킬 2종 + 스턴 시스템 + 구조 확정\*\*



| \*\*파일\*\* | \*\*작업 내용\*\* |

| --- | --- |

| `BioticExplosion.cs` | 신규 생성. 플레이어 중심 원형 폭발, 자동 발동. 스턴 부여 |

| `ElectricEngine.cs` | 신규 생성. 가장 가까운 적 감전 + 주변 적 연쇄 피해 (간단 버전) |

| `EnemyBase.cs` | `IsStunned`, `Stun(duration)` 추가. 풀 재사용 시 스턴 초기화 |

| `EnemyAI.cs` | 스턴 중 이동 정지 처리 추가 |

| `SkillData.cs` | `stunDurationDelta` 추가 (BioticExplosion Lv4용) |

| `SkillEffectApplier.cs` | `BioticExplosion`, `ElectricEngine` 케이스 추가. `testMode` 플래그 추가 |



\*\*구조 확정:\*\* 각 스킬은 독립 컴포넌트로 자체 관리. 선택형 스킬은 시작 시 비활성화 → 레벨업 선택 시 활성화.



\---



\### \*\*추가 수정 사항\*\*



| \*\*내용\*\* | \*\*파일\*\* |

| --- | --- |

| `FixedUpdate` 게임 상태 미체크 버그 수정 + `moveInput` 초기화 | `PlayerController.cs` |

| `Instantiate` 방식 → `ObjectPool` 방식으로 교체 | `ExpOrb.cs`, `EnemyBase.cs`, `ExpOrbPool.cs` |



\---



\*\*현재까지 구현 완료된 스킬 목록\*\*



\- ✅ 휘두르기 (Slash) — 기본 자동 공격

\- ✅ 독침 (PoisonNeedle) — 투사체

\- ✅ 생체 폭발 (BioticExplosion) — 원형 폭발

\- ✅ 전기 기관 (ElectricEngine) — 연쇄 감전

\- ✅ 세포 재생 / 가속 변이 / 신경 가속 — 패시브 3종

